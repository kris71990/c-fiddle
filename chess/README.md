# Chess Game

## Getting Started

To compile: `g++ -o chess.out chess.cpp`

To run: `./chess.out`

## Build with CMake

1. `mkdir build`
2. `cd build` -> 
3. `cmake ../chess`
3. `cmake --build .`
5. `./chess`